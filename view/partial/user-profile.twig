<div class="panel panel-default">

   <div class="panel-heading">會員資料</div>
   <div class="panel-body">
      <p style="text-align: center;">{{ gravatar(200) | raw }}</p>
      <hr>
      <p>使用者：<span class="profile">{{ auth.user.name }}</span></p>
      <p>註冊於：
         <span style="cursor: pointer;" class="profile" data-placement="right" data-toggle="tooltip" title="{{ auth.user.joined_at }}">
            {{ auth.user.joined_ago }}
         </span>
      </p>
      <p>最後登入於：
         <span style="cursor: pointer;" class="profile" data-placement="right" data-toggle="tooltip" title="{{ auth.user.last_login }}">
            {{ auth.user.last_ago }}
         </span>
      </p>
      <hr>
      <a class="btn btn-default plink" href="/changepassword">
         <i class="fa fa-cog mr-7" aria-hidden="true"></i>更改密碼
      </a>
      <a class="btn btn-default plink" href="/update">
         <i class="fa fa-pencil-square-o mr-7" aria-hidden="true"></i>編輯個人資料
      </a>
      {% if auth.user.active == false %}
         <hr>
         <div class="alert alert-warning" role="alert" style="text-align: center;">
            <p style="margin-bottom: 10px;">如要開啟分享功能，請進行電郵驗證</p>
            <form action="/reactive" method="post">
               {{ csrf_field | raw }}
               <button class="btn btn-danger">按此重發驗證碼</button>
            </form>
         </div>
      {% endif %}

   </div>
</div>

<script>
   $('[data-toggle="tooltip"]').tooltip()
</script>